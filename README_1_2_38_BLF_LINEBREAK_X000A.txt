ServusPBX Medical 1.2.38

Änderung:
- BLF-Labels verwenden jetzt den XML-Linefeed &#x000A;
  statt &#10;.

Falls die Firmware das ebenfalls nicht interpretiert,
testen wir als Nächstes einen echten LF (\n) innerhalb
des XML-Inhalts.